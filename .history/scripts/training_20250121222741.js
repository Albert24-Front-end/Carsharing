import { cars } from './cars.js';

function renderCars(cars) {
    const carsContent = docment.querySelector('.car-items');

    carsContent.forEach((car) => {
        const carElement = docment
    });
}